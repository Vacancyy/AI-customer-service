# 参数可视化说明

## 一、显存占用关系图

```
┌─────────────────────────────────────────────────────────────────┐
│                        GPU 显存 (47 GiB)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │    模型权重       │  │    KV Cache      │  │ Forced Align │  │
│  │   (固定占用)      │  │  (max_model_len) │  │ (音频时长)    │  │
│  │                  │  │                  │  │              │  │
│  │  ASR: ~3.5 GiB   │  │  4096: ~0.8 GiB  │  │  每批递增     │  │
│  │  Aligner: ~1 GiB │  │  8192: ~1.6 GiB  │  │              │  │
│  │                  │  │  16384: ~3.2 GiB │  │              │  │
│  └──────────────────┘  └──────────────────┘  └──────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              gpu_memory_utilization = 0.4                │  │
│  │              (模型最多占用 40% 显存)                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  剩余空间 → 用于 Forced Aligner 和计算中间值                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、处理流程与参数关系

```
音频文件 (mp3/)
    │
    ▼
┌─────────────────────────────────────────────────┐
│           阶段1: 说话者分离 (40% 时间)            │
│  ┌───────────────────────────────────────────┐  │
│  │  参数: pyannote 模型 (固定)                │  │
│  │  显存: ~2-3 GiB                           │  │
│  │  断点续传: 每文件保存                      │  │
│  └───────────────────────────────────────────┘  │
│                  ↓ 保存到缓存                    │
│         diarization_cache/*.json                │
└─────────────────────────────────────────────────┘
    │
    ▼ 清理显存
┌─────────────────────────────────────────────────┐
│           阶段2: ASR 转录 (40% 时间)             │
│  ┌───────────────────────────────────────────┐  │
│  │  参数:                                     │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ ASR_BATCH_SIZE = 5                  │  │  │
│  │  │     ↓                               │  │  │
│  │  │ 每批传 5 个文件给 ASR               │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ max_inference_batch_size = 2        │  │  │
│  │  │     ↓                               │  │  │
│  │  │ vLLM 同时处理 2 个请求              │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ max_model_len = 4096                │  │  │
│  │  │     ↓                               │  │  │
│  │  │ KV cache 大小，决定能处理多长的文本  │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │ gpu_memory_utilization = 0.4        │  │  │
│  │  │     ↓                               │  │  │
│  │  │ 模型最多占用 40% 显存               │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  音频分类:                                       │
│  ┌─────────────┐    ┌─────────────┐             │
│  │ 正常音频    │    │ 长音频      │             │
│  │ < 600秒     │    │ > 600秒     │             │
│  │ 每批 5 个   │    │ 每批 1 个   │             │
│  └─────────────┘    └─────────────┘             │
└─────────────────────────────────────────────────┘
    │
    ▼ 清理显存
┌─────────────────────────────────────────────────┐
│           阶段3: API 分类 (20% 时间)             │
│  ┌───────────────────────────────────────────┐  │
│  │  参数:                                     │  │
│  │  api_workers = 20  →  20 线程并发         │  │
│  │  timeout = 120秒   →  请求超时时间        │  │
│  │  max_retries = 3   →  失败重试 3 次       │  │
│  │  temperature = 0.1 →  低温度更稳定        │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  多线程并发:                                     │
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐                 │
│  │ T1│ │ T2│ │ T3│ │...│ │T20│  (20个线程)     │
│  └───┘ └───┘ └───┘ └───┘ └───┘                 │
└─────────────────────────────────────────────────┘
    │
    ▼
  dialog_csv/*.csv
```

---

## 三、参数对显存的影响

```
                        显存占用
                           ↑
                           │
        max_model_len      │      ───────────────
        = 16384           │      ████████████████ (~3.2 GiB)
                           │
        max_model_len      │      ──────────
        = 8192            │      ██████████ (~1.6 GiB)
                           │
        max_model_len      │      ─────
        = 4096            │      █████ (~0.8 GiB) ← 当前设置
                           │
                           └──────────────────────────→ 参数值

                        显存占用
                           ↑
                           │
        ASR_BATCH_SIZE     │
        = 20               │      ████████████████████ (高)
                           │
        ASR_BATCH_SIZE     │
        = 10               │      ████████████ (中)
                           │
        ASR_BATCH_SIZE     │
        = 5                │      ██████ (低) ← 当前设置
                           │
                           └──────────────────────────→ 参数值
```

---

## 四、参数调整对处理能力的影响

```
┌─────────────────────────────────────────────────────────────────┐
│                    max_model_len 与处理能力                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  65536 tokens  ├─────────────────────────────────────────────┤  │
│               │████████████████████████████████████████████████│ │
│               │           可处理 ~32000 中文字                 │ │
│               │           适用: 长文档                         │ │
│                                                                 │
│  16384 tokens  ├─────────────────────────┤                    │ │
│               │███████████████████████████│                    │ │
│               │    可处理 ~8000 中文字     │                    │ │
│               │    适用: 普通对话         │                    │ │
│                                                                 │
│  8192 tokens   ├──────────────────┤                            │ │
│               │████████████████████│                            │ │
│               │  可处理 ~4000 中文字 │                            │ │
│               │  适用: 短对话       │                            │ │
│                                                                 │
│  4096 tokens   ├───────────┤        ← 当前设置                 │ │
│               │█████████████│                                    │ │
│               │可处理 ~2000 中文字                                │ │
│               │适用: 很短对话                                      │ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 五、容错机制流程

```
                    开始处理
                       │
                       ▼
              ┌────────────────┐
              │ 检查缓存是否存在 │
              └────────────────┘
                    │
         ┌─────────┴─────────┐
         │                   │
      存在                  不存在
         │                   │
         ▼                   ▼
   ┌───────────┐      ┌───────────────┐
   │ 加载缓存   │      │ 执行说话者分离 │
   │ 跳过分离   │      │ 每文件保存一次 │
   └───────────┘      └───────────────┘
         │                   │
         └─────────┬─────────┘
                   │
                   ▼
           ┌───────────────┐
           │  ASR 转录      │
           │  批次处理      │
           └───────────────┘
                   │
            ┌──────┴──────┐
            │             │
         成功           失败(OOM)
            │             │
            ▼             ▼
     ┌───────────┐  ┌─────────────────┐
     │ API 分类   │  │ 分离缓存已保存   │
     │ 多线程并发 │  │ 可用 --reuse    │
     └───────────┘  │ 重新运行        │
            │       └─────────────────┘
            ▼
        ┌────────┐
        │  完成   │
        └────────┘
```

---

## 六、时间预估

```
10,000 个文件处理时间分布:

├─────────────────────────────────────┤  总计 ~12.5 小时
│                                     │
│  ████████████████████████████████████████████████████
│  │←──── 分离 5小时 ────→│←─── ASR 5小时 ───→│←API 2.5h→│
│  │       40%           │       40%        │   20%    │
│  │                     │                  │          │
│  │ 瓶颈: 单线程         │ 瓶颈: GPU显存    │ 已优化:   │
│  │ 无法加速            │ 可调参数         │ 多线程    │
│  │                     │                  │          │
└──┴─────────────────────┴──────────────────┴──────────┴───
```

---

## 七、参数快速参考表

```
┌─────────────────────────────┬──────────┬─────────────────────┐
│          参数名              │  当前值  │       作用          │
├─────────────────────────────┼──────────┼─────────────────────┤
│ ASR_BATCH_SIZE              │    5     │ 每批处理文件数      │
│ max_inference_batch_size    │    2     │ vLLM 并行度         │
│ max_model_len               │  4096    │ KV cache 大小       │
│ max_new_tokens              │  2048    │ 输出最大长度        │
│ gpu_memory_utilization      │   0.4    │ 显存占用比例        │
│ LONG_AUDIO_THRESHOLD        │  600秒   │ 长音频阈值          │
│ api_workers                 │   20     │ API 并发线程        │
│ timeout                     │  120秒   │ API 超时            │
│ max_retries                 │    3     │ API 重试次数        │
│ temperature                 │   0.1    │ 生成温度            │
└─────────────────────────────┴──────────┴─────────────────────┘
```